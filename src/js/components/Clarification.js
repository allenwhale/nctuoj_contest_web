import React, { Component } from 'react';
import { Modal, Form, Button } from 'react-bootstrap';

export default class ClarificationForm extends Component {

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                <h2>Clarifiction {this.}</h2>
            </div>
        );
    }
}
